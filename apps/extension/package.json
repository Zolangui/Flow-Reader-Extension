{
  "name": "@flow/extension",
  "private": true,
  "version": "2.0.3",
  "description": "Lumen Read ePub reader as a browser extension.",
  "scripts": {
    "build:chrome": "pnpm -w build:ext:chrome",
    "build:firefox": "pnpm -w build:ext:firefox"
  },
  "devDependencies": {}
}
